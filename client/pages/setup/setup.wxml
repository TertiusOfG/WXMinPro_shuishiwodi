<!--pages/setup/setup.wxml-->
<view class="container">
  <view class="title">游戏设置</view>

  <view class="config-section">
    <view class="config-item">
      <text class="config-label">玩家总人数</text>
      <input class="config-input" type="number" value="{{maxPlayers}}" bindinput="onMaxPlayersInput" />
    </view>

    <view class="config-item">
      <text class="config-label">卧底人数</text>
      <input class="config-input" type="number" value="{{undercoverCount}}" bindinput="onUndercoverCountInput" />
    </view>
    
    <!-- NEW: Hint text for valid range -->
    <view class="hint-text">
      <text>卧底人数范围: {{minUndercover}}-{{maxUndercover}}</text>
    </view>
  </view>

  <input class="nickname-input" placeholder="输入昵称" bindinput="onNicknameInput" value="{{nickname}}" />

  <button class="start-button" type="primary" bindtap="createGame">开 局</button>
</view>
